spring:
  application:
    name: board

  server:
    port: 8080

  datasource:
    url: jdbc:mysql://localhost:3306/easy_home_user
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

